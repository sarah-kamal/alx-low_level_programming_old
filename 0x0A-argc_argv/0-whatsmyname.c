#include<stdio.h>
/**
 * main - a programme that prints itsname followed by new line
 * @argc: number of args
 * @argv: narray of args
 * Return: returns 0
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
